import { Text, ScrollView,StyleSheet, TouchableOpacity, View, Image } from "react-native";

export default function about2(){
    return(
        <view>
            <text>texto do projeto</text>
        </view>
    )

const style= Style.Sheet({
    container: {
        display:'flex',
    },
}
)};
